class CollectorConfiguration:

    def __init__(self, usr: str,
                 password: str,
                 ip_addr: str,
                 port: int):
        self.usr = usr
        self.password = password
        self.ip_addr = ip_addr
        self.port = port
